 for i in range(len(arr) - k + 1):
        subarray = arr[i:i + k]
        if len(set(subarray)) == k:  # Ensure all elements are distinct
            subarrays.append(subarray)
    return subarrays

***** For DISTINCT ELEMENTS ********
